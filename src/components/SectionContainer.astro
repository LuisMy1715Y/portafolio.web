---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 mx-auto w-full max-w-[1280px] px-7 sm:px-10 lg:px-16 xl:px-20`}
>
  <slot />
</section>
